import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const state = {/* root state*/};

const mutations = {/* root mutations */};

const actions = {/* root actions */};

const getters = {/* root getters */};

export default new Vuex.Store({
  state,
  mutations,
  actions,
  getters
});
